using Content.Shared.Implants;

namespace Content.Client.Implants; // namespace clash is intentional for if wizden adds this later

// no idea why this isn't present upstream
public sealed class SubdermalImplantSystem : SharedSubdermalImplantSystem;
